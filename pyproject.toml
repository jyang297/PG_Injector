[project]
name = "pg-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.9"
dependencies = [
    "loguru>=0.7,<0.8",
    "psycopg[binary]>=3.1,<3.3",
]
